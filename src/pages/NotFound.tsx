import InteractiveTerminal from "@/components/InteractiveTerminal";

const NotFound = () => {
  // All routes now show the same terminal interface
  // Users can explore the portfolio through terminal commands
  return <InteractiveTerminal />;
};

export default NotFound;
